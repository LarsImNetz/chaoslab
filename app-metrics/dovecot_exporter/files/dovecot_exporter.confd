# config file for /etc/init.d/dovecot_exporter
# run /usr/bin/dovecot_exporter -h to see available options

DOVECOT_EXPORTER_LOGDIR="/var/log/${RC_SVCNAME}"
DOVECOT_EXPORTER_OPTS=""

# Logging
#output_log="${DOVECOT_EXPORTER_LOGDIR}/dovecot_exporter.log"
#error_log="${DOVECOT_EXPORTER_LOGDIR}/error.log"
