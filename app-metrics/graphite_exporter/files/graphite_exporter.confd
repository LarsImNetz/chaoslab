# config file for /etc/init.d/graphite_exporter
# run /usr/bin/graphite_exporter -h to see available options

GRAPHITE_OPTS=""
GRAPHITE_LOGIDR="/var/log/${RC_SVCNAME}"

# Logging
#output_log="${GRAPHITE_LOGIDR}/graphite_exporter.log"
#error_log="${GRAPHITE_LOGIDR}/error.log"
